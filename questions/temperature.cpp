#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int x, y;
    double tempA, temB;
    cin >> x >> y;
    
    tempA = ;
    if (x == 0 && y == 0){
        printf("IMPOSSIBLE\n")
    }
    else if (tempA != tempB){
        printf("ALL GOOD\nn")
    }
    else{
        printf("%lf\n", tempA)ç
    }
    return 0;
}
