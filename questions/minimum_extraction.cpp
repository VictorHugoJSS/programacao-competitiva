#include <bits/stdc++.h>

using namespace std;

void scan_numbers(int size, int * arr){
    for (int i = 0; i < size; i++){
        scanf("%d", &arr[i]);
    }
}

int main(){
    int TC, i = 0;
    scanf("%d", &TC);

    while (i++ != TC*2){
        int size, j = 0;
        scanf("%d", &size);
        int arr[size];
        scan_numbers(size, arr);
        
    }
}