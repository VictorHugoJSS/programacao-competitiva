#include <bits/stdc++.h>

using namespace std;

double fatorial (char * number){
    int half = sizeof(number)/2;
    char * zeros = (char *) malloc(half * sizeof(char*));
    char * xl = (char *) malloc()
}

int main() {


    while (scanf("%s", x) != EOF){
        cout << x << "!\n";
        cout << fatorial(x) << "\n";
    }

    return 0;
}