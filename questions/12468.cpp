#include <bits/stdc++.h>

using namespace std;

int main(){
    int current, channel, total;

    scanf("%d %d", &current, &channel);

    while (current != -1 && channel != -1){
        total = ;

        printf("%d\n", total);
        scanf("%d %d", &current, &channel);
    }
    
    return 0;
}