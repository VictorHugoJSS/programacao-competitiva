#include <bits/stdc++.h>

using namespace std;

string reverse(string s){
	for (size_t i = 0; i < s.size()/2; i++){
		
	}
}
int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);

	string num1, num2;

	cin >> num1 >> num2;

	
}
