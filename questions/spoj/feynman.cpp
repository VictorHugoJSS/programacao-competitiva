#include <bits/stdc++.h>

using namespace std;

int main(){
    int n;
    int total;
    scanf("%d", &n);

    while (n != 0){
        total = ;
        printf("%d\n", total);
        scanf("%d", &n);
    }
}