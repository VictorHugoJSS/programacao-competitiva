#include <bits/stdc++.h>

using namespace std;

void horizon_move(int *arr, int direction){
    if (direction == 0){
        return;
    }

    if (direction > 0){
        for (int i = 0; i < ; i++){
            for 
        }
    }
}
int main(){
    int m, n;
    scanf("%d %d", &m, &n);

    while (m && n){
        int M[m][n];
        for (int i = 0; i < m; i++){
            for (int j = 0; j < n; j++){
                scanf("%d %d", M[i][j]);
            }
        }
        int x, y;
        scanf("%d %d", &x, &y);

        while (x && y){

        }
    }
}