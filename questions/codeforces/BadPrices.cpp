#include <bits/stdc++.h>

using namespace std;

int main(){
    int tc;

    scanf("%d", &tc);

    while(tc--){
        int size, count = 0;

        scanf("%d", &size);

        vector<int> arr(size);

        for (int i = 0; i < size; i++){
            scanf("%d", &arr[i]);
        }

        for (int i = size-1; i > 0; i--){
            if (){

            }
        }

        
    }
}