#include <bits/stdc++.h>
using namespace std;

int main(){
    int tc;

    scanf("%d", &tc);

    while (tc--){
        int size, temp;
        scanf("%d", &size);
        vector <int> arr;
        for (int i = 0; i < size; i++){
            scanf("%d", &temp);
            arr.push_back(temp);
        }
    }
}