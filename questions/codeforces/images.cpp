#include <bits/stdc++.h>

using namespace std;

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);

	int tc, total = 0;
	int row, col;
	cin >> tc;

	while (tc--){
		cin >> row >> col;
		int v[row][col];
		for (int i = 0; i < row; i++){
			for (int j = 0; j < col; j++){
				cin >> 	v[i][j];
			}
		}

		for (int i = 0; i < rol;i++){
			if ()
		}
		if (total >= ((row * col)/2)){
			cout << "-1\n";
		}
		else{
			cout << total << "\n";
		}
	}
}