#include <bits/stdc++.h>

using namespace std;

void sum(vector<int> &arr){

}
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int tc;

    cin >> tc;

    while (tc--){
        int size, subSize;

        cin >> size >> subSize;

        vector<int> arr(size);

        for (int i = 0; i < size; i++){
            scanf("%d", &arr[i]);
        }

        
    }
}