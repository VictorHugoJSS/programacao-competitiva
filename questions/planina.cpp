#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    int it, total;

    cin >> it;
    
    total = ;

    cout << total << "\n";
    return 0;
}