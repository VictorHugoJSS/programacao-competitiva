#include <bits/stdc++.h>

using namespace std;

int main(){
    int tc;
    scanf("%d", &tc);

    while(tc--){
        int blocks, paper;
        scanf("%d", &blocks);
        
        printf("%d\n", paper);
    }

    return 0;
}