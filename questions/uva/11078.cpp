#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int tc, n;
    cin >> tc;

    while (tc--){
        
        cin >> n;

        for (int i = 0; i < n; i++){

        }
    }
}